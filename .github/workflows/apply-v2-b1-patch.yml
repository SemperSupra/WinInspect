name: Apply V2-B1 qualification patch

on:
  push:
    branches:
      - experiment/375-daemon-shutdown-lifecycle-handoff
    paths:
      - .qualification/v2-b1.patch

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Validate and apply patch
        shell: bash
        run: |
          set -euo pipefail
          git apply --check --recount .qualification/v2-b1.patch
          git apply --recount .qualification/v2-b1.patch
          git diff --check

      - name: Remove temporary applicator
        shell: bash
        run: |
          set -euo pipefail
          rm -f .qualification/v2-b1.patch
          rm -f .github/workflows/apply-v2-b1-patch.yml
          rmdir .qualification 2>/dev/null || true

      - name: Commit exact patched tree
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix: delegate daemon shutdown to lifecycle owner"
          git push origin HEAD:${GITHUB_REF_NAME}
